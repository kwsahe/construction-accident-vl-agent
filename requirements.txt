# Python 3.11.15
#
# Core local VL agent runtime:
# - read mp4 files
# - extract frames
# - build contact sheet images
# - call the remote Qwen2.5-VL OpenAI-compatible endpoint via stdlib urllib
opencv-python>=4.9,<5
Pillow>=10.2,<12
python-multipart>=0.0.9,<1
uvicorn[standard]>=0.30,<1
fastapi>=0.115,<1
yt-dlp>=2026.6.9
ultralytics>=8.3,<27

# Optional: enable direct Django ORM writes with `--insert-db`.
# Not required when only generating JSON payloads or using `--api-url`.
# Django>=4.2,<6
